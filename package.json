{
  "name": "karasu",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "overrides": {
    "dompurify": "^3.3.2"
  },
  "scripts": {
    "dev": "pnpm --filter @karasu-tools/app run dev",
    "build": "pnpm --filter @karasu-tools/core run build && pnpm --filter @karasu-tools/lsp run build && pnpm --filter @karasu-tools/app run build && pnpm --filter ./packages/cli run build",
    "test": "pnpm --filter @karasu-tools/core run test && pnpm --filter @karasu-tools/app run test && pnpm --filter ./packages/cli run test && pnpm --filter @karasu-tools/lsp run test",
    "test:coverage": "pnpm --filter @karasu-tools/core run test:coverage && pnpm --filter @karasu-tools/app run test:coverage && pnpm --filter ./packages/cli run test:coverage",
    "lint": "oxlint --deny-warnings packages/",
    "lint:fix": "oxlint --fix packages/",
    "format": "oxfmt --write packages/",
    "format:check": "oxfmt --check packages/",
    "typecheck": "pnpm -r run typecheck",
    "knip": "knip"
  },
  "devDependencies": {
    "knip": "^6.6.0",
    "lefthook": "^2.1.6",
    "oxfmt": "^0.45.0",
    "oxlint": "1.60.0"
  }
}
