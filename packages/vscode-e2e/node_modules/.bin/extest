#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/workspaces/karasu/.claude/worktrees/spike/2761-width-budget-ladder/node_modules/.pnpm/vscode-extension-tester@8.27.0_mocha@11.8.0_supports-color@8.1.1_typescript@7.0.2/node_modules/vscode-extension-tester/node_modules:/workspaces/karasu/.claude/worktrees/spike/2761-width-budget-ladder/node_modules/.pnpm/vscode-extension-tester@8.27.0_mocha@11.8.0_supports-color@8.1.1_typescript@7.0.2/node_modules:/workspaces/karasu/.claude/worktrees/spike/2761-width-budget-ladder/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/workspaces/karasu/.claude/worktrees/spike/2761-width-budget-ladder/node_modules/.pnpm/vscode-extension-tester@8.27.0_mocha@11.8.0_supports-color@8.1.1_typescript@7.0.2/node_modules/vscode-extension-tester/node_modules:/workspaces/karasu/.claude/worktrees/spike/2761-width-budget-ladder/node_modules/.pnpm/vscode-extension-tester@8.27.0_mocha@11.8.0_supports-color@8.1.1_typescript@7.0.2/node_modules:/workspaces/karasu/.claude/worktrees/spike/2761-width-budget-ladder/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../vscode-extension-tester/out/cli.js" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../vscode-extension-tester/out/cli.js" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../vscode-extension-tester/out/cli.js" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../vscode-extension-tester/out/cli.js" "$@"
else
  exec node  "$basedir/../vscode-extension-tester/out/cli.js" "$@"
fi
# cmd-shim-target=/workspaces/karasu/.claude/worktrees/spike/2761-width-budget-ladder/packages/vscode-e2e/node_modules/vscode-extension-tester/out/cli.js
