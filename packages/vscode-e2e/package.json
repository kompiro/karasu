{
  "name": "@karasu-tools/vscode-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "E2E tests for the karasu VS Code extension: extension host smoke tests, the ExTester WebView E2E harness, and the Marketplace screenshot capture generator",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "pnpm --filter @karasu-tools/core run build && pnpm --filter @karasu-tools/i18n run build && pnpm --filter @karasu-tools/lsp run build && pnpm --filter karasu-vscode run build && pnpm run build",
    "test": "vscode-test",
    "test:webview": "pnpm run pretest && node run-webview-tests.mjs",
    "capture:screenshots": "pnpm run pretest && node capture-screenshots.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.2.0",
    "@types/vscode": "^1.125.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.9.2",
    "mocha": "^11.8.0",
    "typescript": "^7.0.2",
    "vscode-extension-tester": "^8.24.0"
  }
}
