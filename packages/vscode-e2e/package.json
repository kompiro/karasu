{
  "name": "@karasu-tools/vscode-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "VS Code extension host smoke tests for the karasu extension",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "pnpm --filter @karasu-tools/core run build && pnpm --filter @karasu-tools/lsp run build && pnpm --filter karasu-vscode run build && pnpm run build",
    "test": "vscode-test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.116.0",
    "@vscode/test-cli": "^0.0.10",
    "mocha": "^11.7.4",
    "typescript": "^6.0.3"
  }
}
