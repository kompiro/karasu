# SVG / PNG エクスポート

## 概要

描画した図を SVG または PNG ファイルとしてエクスポートする。

## 要件

- SVG エクスポート：ベクター形式、フォント埋め込み
- PNG エクスポート：任意の解像度（1x / 2x / 4x）
- エクスポート範囲：表示中の図全体 / 選択範囲

## 未決定事項

- フォントの埋め込み方法（SVG の場合）
- 背景色の扱い（透過 or 白）
- ファイル名の命名規則（図のラベルを使う？）
