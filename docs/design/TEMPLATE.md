<!--
このファイルは docs/design/ 配下の Design Doc を新規作成するときの雛形。

使い方:
1. このファイルを `docs/design/<kebab-case-name>.md` にコピーする。
2. コピー先で本 HTML コメントブロック（先頭）を削除し、各セクションを埋める。
3. 不要なセクションは削除してよい（特に「現状（インベントリ）」「未解決の問い」は任意）。
4. ADR に昇格したら、コピーした Design Doc ファイルは **同じ PR で削除** する
   （`docs/process.md` の「設計判断を ADR に残すタイミング」節および
   `.claude/rules/adr.md` の「Design Doc → ADR への昇格」節を参照）。

規約メモ:
- ファイル名: `docs/design/<kebab-case-name>.md`。ADR 昇格時のファイル名は
  `docs/adr/YYYYMMDD-NN-<same-name>.md`（`.claude/rules/adr.md`）。
- 言語: 日本語本文 + 英語の識別子・固有名詞（ADR と同じポリシー、
  `.claude/rules/adr.md`）。flowery な日本語は避け、後で機械的に英訳できる
  程度に抑える。
- TPL 確認: Design Doc 作成時は `docs/test-perspectives/` の既存 TPL と
  `docs/concepts.ja.md` ・関連 ADR をスキャンし、該当する観点があれば
  「関連」節に引用する（`CLAUDE.md` の TPL 参照ルール）。proactive TPL が
  必要なら同じ PR で起こす。
- このテンプレート自体は ADR と違ってバリデータを持たない。フォーマットの
  ズレは PR レビューで吸収する。
-->

# <短く人間が読めるタイトル>

- **日付**: YYYY-MM-DD
- **ステータス**: 検討中 <!-- | 決定済み（[ADR-YYYYMMDD-NN](../adr/YYYYMMDD-NN-...md) として昇格） | 取りやめ -->
- **関連**:
  - 引き金 Issue: [#NNNN](https://github.com/kompiro/karasu/issues/NNNN)
  - 関連 ADR: [ADR-YYYYMMDD-NN](../adr/YYYYMMDD-NN-...md)
  - 関連 TPL: [TPL-YYYYMMDD-NN](../test-perspectives/YYYYMMDD-NN-...md) <!-- 該当があれば。proactive TPL を同 PR で起こす場合もここに -->
  - コード: `packages/<package>/src/<path>.ts`

## 背景・課題

<!-- なぜこの設計を検討するに至ったか。困っている具体例・観測した症状・
     直近の Issue や bug。読者がこの節だけ読んで「何を解こうとしているか」
     が分かることを目指す。 -->

## 現状（インベントリ）

<!-- 任意。関連コード・既存仕様の現状を箇条書きまたは表で。
     設計の前提を読者と揃えるための節。コードを読まないと分からない
     事実（どのファイルが何をしているか、既存 API の signature、
     既知の制約）をここに集める。

     例（`style-ast-shape.md` より）:
     | 観点 | 現状 |
     | --- | --- |
     | token / lexer | 空白・コメントは skip。各 token に loc を持つ |
     | parser | parseValue が `;` `}` `EOF` まで貪欲 |
-->

## 制約・前提

<!-- 後方互換・影響範囲・パフォーマンス・依存関係など、解の選択肢を
     縛る条件。「これは out of scope」も明示する。 -->

- <制約1>
- <制約2>

## 検討した選択肢

### 案1: <短い名前>

<内容の説明>

**メリット**

- <利点1>

**デメリット**

- <欠点1>

### 案2: <短い名前>

<!-- 案3 以降は必要に応じて。比較対象が 1 つしかない場合は本節を省略して
     「現時点の方針」だけで構わない。 -->

## 比較

<!-- 任意。選択肢が 3 つ以上ある、または評価軸が複数ある場合に有用。
     観点は「変更量」「後方互換性」「ユーザー体験」「実装コスト」など。 -->

| 観点 | 案1 | 案2 |
| --- | --- | --- |
| 変更量 | | |
| 後方互換性 | | |
| <その他> | | |

## 現時点の方針

<!-- どの案を採るか + 採用理由を 1〜2 段落で。レビューで合意が固まったら
     「確定した方針」節を別に立てるか、本節を書き換える。 -->

**案N を採用する** — <理由>

### 実装の指針

<!-- 採用案を実装するときの具体ステップ。コード変更点・新規ファイル・
     既存ファイルの修正方針・テストの追加・ドキュメント更新を含める。 -->

1. <ステップ1>
2. <ステップ2>
3. AT: `docs/acceptance/` に新規ファイル。TC は:
   - <観点1>
   - <観点2>
4. ADR 昇格: 実装完了後、`docs/adr/YYYYMMDD-NN-<name>.md` として昇格し、
   本 Design Doc は同 PR で削除する。

### 影響範囲・マイグレーション

<!-- 既存ユーザーへの影響・ドキュメント更新箇所・既存テストデータへの影響を
     列挙する。「特になし」と書いて済むなら 1 行で構わない。 -->

- 既存ユーザーへの影響: <影響の説明 or 「なし」>
- ドキュメント更新: `docs/spec/...`, `docs/concepts.md` 等
- テスト・examples への影響: <影響の説明 or 「なし」>

## 未解決の問い / 決めないこと

<!-- 任意。本 Design Doc では意図的に決めない範囲を明示する。
     フェーズ分けして将来検討する項目を残すときに有用
     （例: `style-ast-shape.md` のフェーズ 2/3）。 -->

- <未解決1>
- <意図的に決めない事項とその理由>
