---
"karasu": minor
---

First release of the `karasu` CLI to npm. The CLI is now built as a single self-contained ESM bundle (`@karasu-tools/core` is bundled in via esbuild), and releases are managed with changesets.
