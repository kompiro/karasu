---
"@karasu-tools/core": minor
"karasu": minor
---

Add a job lane to the deploy view (first kind band). Deploy units are grouped into a dedicated lane by kind, giving the deploy view a structured first band for job-style workloads. See #1749.
