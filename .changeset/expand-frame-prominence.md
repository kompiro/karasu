---
"@karasu-tools/core": patch
"karasu": patch
---

Render an in-place-expanded container's frame prominently — a solid accent border with a faint tint — so an opened service stands out instead of blending into a busy diagram as the muted dashed team-frame style did (#1921 feedback).
